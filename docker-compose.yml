# =============================================================================
# Post-Quantum VPN Router (Open Source)
# =============================================================================
# MIT License - Full source code available
# Enterprise support: grant@abejar.net
# =============================================================================

services:
  vpn-router:
    build:
      context: ./vpn-router
      dockerfile: Dockerfile
    image: pqc-vpn-router:latest
    container_name: vpn-router
    hostname: vpn-router
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
      - PQC_ENABLED=${PQC_ENABLED:-true}
      - PQC_ALGORITHM=${PQC_ALGORITHM:-kyber1024}
      - PQC_HYBRID_MODE=${PQC_HYBRID_MODE:-true}
      - PQC_KEY_ROTATION_HOURS=${PQC_KEY_ROTATION_HOURS:-24}
      - VPN_INTERFACE=${VPN_INTERFACE:-wg0}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - LOG_FORMAT=${LOG_FORMAT:-json}
    volumes:
      - ./vpn-router/config/wireguard:/config/wg_confs:rw
      - ./vpn-router/config/routing:/config/routing:ro
      - /lib/modules:/lib/modules:ro
    ports:
      - "127.0.0.1:${APP_PORT_START:-10091}-${APP_PORT_END:-10100}:${APP_PORT_START:-10091}-${APP_PORT_END:-10100}"
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
      - net.ipv4.ip_forward=1
      - net.ipv6.conf.all.forwarding=1
    networks:
      vpn-network:
        ipv4_address: ${VPN_ROUTER_IP:-172.25.0.2}
    healthcheck:
      test: ["CMD", "/healthcheck.sh"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    labels:
      - "com.pqc.enabled=true"
      - "com.pqc.algorithm=kyber1024"
      - "com.pqc.license=MIT"

  cloudflared:
    build:
      context: ./cloudflared
      dockerfile: Dockerfile
    image: pqc-cloudflared:latest
    container_name: cloudflare-tunnel
    network_mode: "service:vpn-router"
    environment:
      - TUNNEL_TOKEN=${CF_TUNNEL_TOKEN}
      - TUNNEL_LOGLEVEL=${CF_LOG_LEVEL:-info}
      - TUNNEL_POST_QUANTUM=${CF_TUNNEL_PQC:-true}
    depends_on:
      vpn-router:
        condition: service_healthy
    restart: unless-stopped
    labels:
      - "com.pqc.enabled=true"
      - "com.pqc.component=cloudflared"

networks:
  vpn-network:
    name: pqc_vpn_network
    driver: bridge
    ipam:
      config:
        - subnet: ${VPN_NETWORK_CIDR:-172.25.0.0/16}
          gateway: ${VPN_NETWORK_GATEWAY:-172.25.0.1}

volumes:
  vpn-config:
    name: pqc_vpn_config
